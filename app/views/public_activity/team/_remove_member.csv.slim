- removed_user = activity.recipient&.display_username || activity.parameters[:team_user]
= CSV.generate_line(['team', activity_team(activity), 'remove member', removed_user, activity_owner(activity), activity.created_at, "role #{activity.parameters[:role]}"])
