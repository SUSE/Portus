.panel.panel-default
  .panel-heading
    h5
      | All activities
      .pull-right
        a.btn.btn-link.btn-xs[role="button" href="#{url_for(admin_activities_path(format: :csv))}" title="Download in CSV format"]
          i.fa.fa-download
          | Download CSV
  .panel-body
    ul.activity
      - @activities.each do |activity|
        = render_activity(activity)
    = paginate(@activities)
