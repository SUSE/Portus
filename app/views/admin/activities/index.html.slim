h1
  .pull-left
    | Activities
  .pull-right
    a.btn.btn-primary[role="button" href="#{url_for(admin_activities_path(format: :csv))}" title="Download in CSV format"]
      i.fa.fa-download
  .clearfix

- @activities.each do |activity|
  = render_activity(activity)
