web:
  extends:
    file: docker-compose.overlay.yml
    service: web
  links:
    - db

crono:
  extends:
    file: docker-compose.overlay.yml
    service: crono
  links:
    - db

db:
  extends:
    file: docker-compose.overlay.yml
    service: db

registry:
  extends:
    file: docker-compose.overlay.yml
    service: registry
  links:
    - web
